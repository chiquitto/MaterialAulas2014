% \documentclass{beamer}
\documentclass[aspectratio=32]{beamer}

%%%%%%%%% ALISSON CHIQUITTO

\usepackage[brazil]{babel}
\usepackage[utf8]{inputenc}
%\usepackage[T1]{fontenc}
%\usepackage[hyphens]{url}
%\usepackage{graphicx}
%\usepackage[pdftex]{graphicx}
%\usepackage{epstopdf}
\usepackage{cite}
%\usepackage{color}
\usepackage{enumerate}
%\usepackage{caption}
%\usepackage{hyperref}

% Definir o caminho das figuras
%\DeclareGraphicsExtensions{.png,.pdf}
%\graphicspath{{img/}}

%%%%%%%%% /ALISSON CHIQUITTO

% There are many different themes available for Beamer. A comprehensive
% list with examples is given here:
% http://deic.uab.es/~iblanes/beamer_gallery/index_by_theme.html
% You can uncomment the themes below if you would like to use a different
% one:
%\usetheme{AnnArbor}
%\usetheme{Antibes}
%\usetheme{Bergen}
%\usetheme{Berkeley}
%\usetheme{Berlin}
%\usetheme{Boadilla}
%\usetheme{boxes}
%\usetheme{CambridgeUS}
%\usetheme{Copenhagen}
%\usetheme{Darmstadt}
%\usetheme{default}
%\usetheme{Frankfurt}
%\usetheme{Goettingen}
%\usetheme{Hannover}
%\usetheme{Ilmenau}
%\usetheme{JuanLesPins}
%\usetheme{Luebeck}
\usetheme{Madrid}
%\usetheme{Malmoe}
%\usetheme{Marburg}
%\usetheme{Montpellier}
%\usetheme{PaloAlto}
%\usetheme{Pittsburgh}
%\usetheme{Rochester}
%\usetheme{Singapore}
%\usetheme{Szeged}
%\usetheme{Warsaw}

\title{HTML}

% A subtitle is optional and this may be deleted
\subtitle{Conceitos, tags e atributos básicos}

% \author{F.~Author\inst{1} \and S.~Another\inst{2}}
\author{Alisson Chiquitto\inst{1}}
% - Give the names in the same order as the appear in the paper.
% - Use the \inst{?} command only if the authors have different
%   affiliation.

\institute[UEM] % (optional, but mostly needed)
{
  \inst{1}%
  Curso de Informática
}

% \institute[Universities of Somewhere and Elsewhere] % (optional, but mostly needed)
% {
%   \inst{1}%
%   Department of Computer Science\\
%   University of Somewhere
%   \and
%   \inst{2}%
%   Department of Theoretical Philosophy\\
%   University of Elsewhere}
% - Use the \inst command only if there are several affiliations.
% - Keep it simple, no one is interested in your street address.

\date{Cianorte, 2014}
% - Either use conference name or its abbreviation.
% - Not really informative to the audience, more for people (including
%   yourself) who are reading the slides online

%\subject{Theoretical Computer Science}
% This is only inserted into the PDF information catalog. Can be left
% out. 

% If you have a file called "university-logo-filename.xxx", where xxx
% is a graphic format that can be processed by latex or pdflatex,
% resp., then you can add a logo as follows:

% \pgfdeclareimage[height=0.5cm]{university-logo}{university-logo-filename}
% \logo{\pgfuseimage{university-logo}}

% Delete this, if you do not want the table of contents to pop up at
% the beginning of each subsection:
\AtBeginSubsection[]
{
  \begin{frame}<beamer>{Outline}
    \tableofcontents[currentsection,currentsubsection]
  \end{frame}
}

% Adapted from beamerinnerthemedfault.sty
% http://tex.stackexchange.com/questions/11168/change-bullet-style-formatting-in-beamer
% \setbeamertemplate{itemize item}{\scriptsize\raise1.25pt\hbox{\donotcoloroutermaths$\blacktriangleright$}}
% \setbeamertemplate{itemize subitem}{\tiny\raise1.5pt\hbox{\donotcoloroutermaths$\blacktriangleright$}}
% \setbeamertemplate{itemize subsubitem}{\tiny\raise1.5pt\hbox{\donotcoloroutermaths$\blacktriangleright$}}
\setbeamertemplate{enumerate item}{\insertenumlabel.}
\setbeamertemplate{enumerate subitem}{\insertenumlabel.\insertsubenumlabel}
\setbeamertemplate{enumerate subsubitem}{\insertenumlabel.\insertsubenumlabel.\insertsubsubenumlabel}
\setbeamertemplate{enumerate mini template}{\insertenumlabel}

% 100px = 0.64in
% 200px = 1.28in - 250px = 1.6in
% 300px = 1.92in - 350px = 2.24in
% 400px = 2.56in - 450px = 2.88in
% 500px = 3.2in
\pgfdeclareimage[width=1.92in]{fig:exemplo-1}{img/exemplo-1.jpg}
\pgfdeclareimage[width=2.56in]{fig:html-basico}{img/html-basico.jpg}
\pgfdeclareimage[width=2.56in]{fig:paragrafos}{img/paragrafos.jpg}
\pgfdeclareimage[width=2.88in]{fig:enfase}{img/enfase.jpg}
\pgfdeclareimage[width=3.2in]{fig:img}{img/img.jpg}
\pgfdeclareimage[width=2.56in]{fig:ancora}{img/ancora.jpg}

% Let's get started
\begin{document}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Outline}
  \tableofcontents
  % You might wish to add the option [pausesections]
\end{frame}

% =======

\section{Introdução}

\begin{frame}{O que é o Hipertexto?}
  
  \begin{block}{Hipertexto}Hipertexto é o termo que remete a um texto em formato digital (...) cujo acesso se dá através de referências denominadas \textbf{hiperlinks}, ou simplesmente \textbf{links}. Esses links ocorrem na forma de termos destacados no corpo de texto (...) oferecendo acesso as informações que estendem ou complementam o texto principal.\end{block}
  
\end{frame}

\begin{frame}{O que é uma linguagem de marcação?}

  \begin{block}{Linguagem de marcação}Linguagem de marcação, é um sistema para anotação de um texto de modo que ele seja sintaticamente distinguível.\end{block}

  
\end{frame}

\begin{frame}{O que é o HTML?}

  \begin{block}{HTML}\textit{HyperText Markup Language} (\textit{HTML}), ou em português, Linguagem de Marcação de Hipertexto é uma linguagem de marcação utilizada para produzir páginas na Web.\end{block}
  
\end{frame}

\section{HTML}

\subsection{Sintaxe do HTML}

\begin{frame}{Sintaxe do HTML}

\textit{HTML} é um conjunto de \textbf{tags} responsáveis pela marcação do conteúdo de uma página no navegador;

\begin{figure}
\pgfuseimage{fig:exemplo-1}
\caption{Um texto marcado como título e uma imagem}
\end{figure}
  
\end{frame}

\begin{frame}{Tags do HTML}

  \begin{itemize}
    \item {
      Note a sintaxe do exemplo anterior: uma \textit{tag} é definida com os caracteres $<$ e $>$, e o seu nome (neste caso \textit{h1} e \textit{img});
    }
    \item {
      Muitas tags possuem conteúdo, como é o caso da \textit{tag} \textit{h1}. Neste caso para determinar onde o conteúdo acaba, usamos uma tag de fechamento com a barra antes do nome. Ex.: $<$/h1$>$
    }
  \end{itemize}
  
\end{frame}

\begin{frame}{Atributos das tags HTML}

  \begin{itemize}
    \item {
      Algumas tags podem receber \textbf{atributos} dentro da sua definição. São parametros usando a sintaxe \textbf{\textit{nome=``valor''}}.
    }
    \item {
      Para definir uma imagem, usamos a tag \textit{img}, e para indicar qual imagem carregar usamos o atributo \textit{src};
    }
  \end{itemize}
  
\end{frame}

\begin{frame}{Identação de código}

  \begin{block}{Identação de código}
    Uma prática sempre recomendada, ligada à limpeza e utilizada para facilitar a leitura do código, é o uso correto de recuos, ou indentação, no HTML. Costumamos alinhar elementos ``irmãos'' na mesma margem e adicionar alguns espaços ou um tab para elementos ``filhos''.
  \end{block}
  
\end{frame}

\subsection{Estrutura de um documento HTML}

\begin{frame}{Estrutura básica do HTML}

  \begin{itemize}
    \item {
      Um documento HTML válido precisa seguir obrigatoriamente a estrutura composta pelas tags $<$html$>$, $<$head$>$ e $<$body$>$ e a instrução $<$!DOCTYPE$>$.
    }
  \end{itemize}
  
\begin{figure}
\pgfuseimage{fig:html-basico}
\caption{Estrutura básica de um documento HTML}
\end{figure}
  
\end{frame}

\begin{frame}{A instrução DOCTYPE}

  \begin{itemize}
    \item {
      O \textit{DOCTYPE} não é uma tag \textit{HTML};
    }
    \item {
      Ela indica para o navegador qual versão do \textit{HTML} deve ser utilizada para renderizar a página;
    }
    \item {
      Utilize $<$!DOCTYPE html$>$ para indicar que esta usando a versão 5 (a mais atual) do \textit{HTML};
    }
  \end{itemize}
  
\end{frame}

\begin{frame}{A tag html}

  \begin{itemize}
    \item {
      Na estrutura do nosso documento, antes de tudo, inserimos uma tag $<$html$>$. Dentro dessa tag, é necessário declarar outras duas tags: $<$head$>$ e $<$body$>$.
    }
  \end{itemize}
  
\end{frame}

\begin{frame}{A tag head}

  \begin{itemize}
    \item {
      A tag $<$head$>$ contém informações sobre nosso documento que são de interesse somente do navegador, e não dos visitantes do nosso site. São informações que não serão exibidas na área do documento no navegador.
    }
    \item {
      A especificação obriga a presença da tag de conteúdo $<$title$>$ dentro do nosso $<$head$>$, permitindo especificar o título do nosso documento;
    }
  \end{itemize}
  
\end{frame}

\begin{frame}{A tag head: encoding/charset}

  \begin{itemize}
    \item {
      Uma configuração muito utilizada é o \textit{encoding}/\textit{charset}, principalmente em documentos cujo conteúdo é escrito com idioma como o português, que contém caracteres como acentos e cedilhas;
    }
    \item {
      Podemos fazer esta configuração através da tag $<$meta$>$;
    }
    \item {
      O \textbf{UTF-8} é a recomendado para \textit{encoding} na web por ser amplamente suportada por navegadores, além de ser compatível com praticamente todos os idiomas do mundo;
    }
  \end{itemize}
  
\end{frame}

\begin{frame}{A tag body: o corpo do documento}

  \begin{itemize}
    \item {
      A tag $<$body$>$ contém o corpo do nosso documento, que é exibido pelo navegador em sua janela.
    }
  \end{itemize}
  
\end{frame}

\subsection{Tags básicas HTML}

\begin{frame}{Títulos}

  \begin{itemize}
    \item {
      Quando queremos indicar que um texto é um título em nossa página, utilizamos as \textit{tags} de \textit{heading} em sua marcação;
    }
    \item {
      As tags de heading são tags de conteúdo e vão de $<$h1$>$ a $<$h6$>$, seguindo a ordem de importância, sendo $<$h1$>$ o título principal, o mais importante, e $<$h6$>$ o título de menor importância.
    }
  \end{itemize}
  
\end{frame}

\begin{frame}{Parágrafos}

  \begin{itemize}
    \item {
      Quando exibimos qualquer texto em nossa página, é recomendado que ele seja sempre conteúdo de alguma tag;
    }
    \item {
      A marcação mais indicada para textos comuns é a tag de parágrafo;
    }
  \end{itemize}
  
\begin{figure}
\pgfuseimage{fig:paragrafos}
\caption{Paragrafos em HTML}
\end{figure}
  
\end{frame}

\begin{frame}{Links}

  Quando precisamos indicar que um trecho de texto se refere a um outro conteúdo, seja ele no mesmo documento ou em outro endereço, utilizamos a tag de âncora $<$a$>$;
  
\begin{figure}
\pgfuseimage{fig:ancora}
\caption{Hipelinks com HTML}
\end{figure}
  
\end{frame}

\begin{frame}{Marcações de ênfase}

  Quando queremos dar uma ênfase diferente a um trecho de texto, podemos utilizar as \textbf{marcações de ênfase}. Podemos deixar um texto \textit{mais forte} com a tag $<$strong$>$ ou deixar o texto com uma \textit{ênfase acentuada} com a tag $<$em$>$.

\begin{figure}
\pgfuseimage{fig:enfase}
\caption{Textos com ênfase}
\end{figure}
  
\end{frame}

\begin{frame}{Imagens}

  \begin{itemize}
    \item {
      A tag $<$img$>$ define uma imagem em uma página HTML e necessita de dois atributos preenchidos: \textit{src} e \textit{alt}.
    }
    \item {
      O atributo \textit{src} aponta para o local da imagem;
    }
    \item {
      O atributo \textit{alt} é um texto alternativo para a imagem caso essa não possa ser carregada ou visualizada;
    }
  \end{itemize}
  
\begin{figure}
\pgfuseimage{fig:img}
\caption{Tag responsável por adicionar uma imagem ao documento}
\end{figure}
  
\end{frame}

\section{Atividades}

\begin{frame}{Atividade 1: Hello World}

  \begin{enumerate}

    \item {
      Criar um arquivo chamado \textbf{base.html}, e criar a estrutura básica do HTML;
    }
    \item {
      O título do documento deverá ser \textit{Hello World};
    }
    \item {
      Adicionar um parágrafo com o texto \textbf{Hello World};
    }
    \item {
      Abrir o arquivo no navegador para ver o resultado;
    }
    
  \end{enumerate}
  
\end{frame}

\begin{frame}{Atividade 2: Mussum Ipsum}

  \begin{enumerate}

    \item {
      Fazer uma cópia de \textbf{base.html}, e salvar como \textbf{mussum-ipsum.html};
    }
    \item {
      Mudar o título do documento para \textbf{Mussum Ipsum};
    }
    \item {
      Adicionar um título \textbf{Mussum Ipsum} com a ajuda das \textit{tags} de título;
    }
    \item {
      Adicionar o texto Mussum Ipsum (que esta no arquivo \textit{mussum-ipsum.txt}) ao documento. Não esquecer de marcar os parágrafos corretamente;
    }
    
  \end{enumerate}
  
\end{frame}

\begin{frame}{Atividade 3: Loja de brinquedos}

  \begin{enumerate}

    \item {
      Fazer uma cópia de \textbf{base.html}, e salvar como \textbf{loja-brinquedos.html};
    }
    \item {
      Mudar o título do documento para \textbf{Mirror Toys};
    }
    \item {
      Adicionar o texto que esta no arquivo \textit{sobre.txt} ao documento. Não esquecer de marcar os paragrafos e títulos corretamente;
    }
    \item {
      Adicionar as imagens contidas no diretorio material ao documento;
    }
    
  \end{enumerate}
  
\end{frame}

\begin{frame}{Atividade 4: Links}

  \begin{enumerate}

    \item {
      Fazer uma cópia de \textbf{base.html}, e salvar como \textbf{links.html};
    }
    \item {
      Criar o link \textbf{Hello World} que aponta para o arquivo \textbf{base.html};
    }
    \item {
      Criar o link \textbf{Mussum Ipsum} que aponta para o arquivo \textbf{mussum-ipsum.html};
    }
    \item {
      Criar o link \textbf{Mirror Toys} que aponta para o arquivo \textbf{loja-brinquedos.html};
    }
    
  \end{enumerate}
  
\end{frame}

% =============================================================================
% =============================================================================
% =============================================================================

% All of the following is optional and typically not needed. 
% \appendix
\section<presentation>*{\appendixname}
\subsection<presentation>*{Referências}

\begin{frame}[allowframebreaks]
  \frametitle<presentation>{Referências}
    
  \begin{thebibliography}{3}
  
  \setbeamertemplate{bibliography item}{\includegraphics[width=1.5em]{img/www.png}}
  
  \bibitem{HTML5Definition}
    HTML5 Definition Complete, W3C Moves to Interoperability Testing and Performance.
    \newblock {\em W3C}.
    \newblock \url{http://www.w3.org/2012/12/html5-cr}
  
  \bibitem{HipertextoWiki}
    Hipertexto
    \newblock {\em Wikipedia}.
    \newblock \url{http://pt.wikipedia.org/wiki/Hipertexto}
    
  \bibitem{LinguagemMarcacaoWiki}
    Linguagem de Marcação
    \newblock {\em Wikipedia}.
    \newblock \url{http://pt.wikipedia.org/wiki/Linguagem_de_marca\%C3\%A7\%C3\%A3o}
    
  \bibitem{LinguagemMarcacaoPE}
    Linguagem de Marcação
    \newblock {\em Portal Educação}.
    \newblock \url{http://www.portaleducacao.com.br/educacao/artigos/31639/linguagem-de-marcacao}
  
  \bibitem{LinguagemMarcacaoCelio}
    Introdução a Linguagens de Marcação: HTML, XHTML, SGML, XML
    \newblock {\em Prof. Célio Guimarães - Instituto de Computação - Unicamp}.
    \newblock \url{http://www.ic.unicamp.br/~celio/inf533/docs/markup.html}
    
  \end{thebibliography}
\end{frame}

\end{document}

